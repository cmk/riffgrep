# Plan 02 — Delete dormant Claude auto-review workflow

## Goal

Delete `.github/workflows/claude-code-review.yml`. The workflow has
been gated by `if: false` since it was added in PR #2 (2026-04-15)
with a comment saying it would be re-enabled once
`CLAUDE_CODE_OAUTH_TOKEN` was set; the secret has actually been set
since 2026-04-16. Three weeks of dormant skipped runs is enough
signal that the auto-review path isn't wanted — Copilot reviews
every PR already.

The companion `claude.yml` (mention-triggered `@claude` bot) stays
untouched.

## Dependency Graph

```
T1 (rm workflow) -> T2 (verify CI green)
```

## Tasks

### T1 — Remove the workflow

- `git rm .github/workflows/claude-code-review.yml`.

### T2 — Verify

- Push the branch; the `Claude Code Review` check should no longer
  appear in PR checks. `Check & Test` and `Python tests` keep their
  green runs.

## Verification

### Properties (must pass)

No new proptest properties — workflow deletion only.

| Property | Module | Invariant |
|---|---|---|
| (n/a) | — | — |

### Spot checks

- `gh pr checks <N>` on this PR no longer lists
  `claude-review` / `Claude Code Review`.
- `claude.yml` is untouched and still listed as a workflow.

### Build gates

- CI's `Check & Test` and `Python tests` jobs stay green.

## Deferred

- Whether to also delete `claude.yml` is left for a separate
  decision — it is mention-triggered and harmless when no one says
  `@claude`. Deferred per user direction.

## Review

Filled in at finalization.
